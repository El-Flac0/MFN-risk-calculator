# Netlify configuration — Medicare Drug Spending + International Prices
[functions]
  directory = "netlify/functions"

# Proxy CMS API requests to avoid CORS issues
[[redirects]]
  from = "/api/*"
  to = "https://data.cms.gov/:splat"
  status = 200
  force = true
